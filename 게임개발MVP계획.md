# Bit Flow MVP 개발 계획

## 요약

- Next.js(App Router·TypeScript·Tailwind·Turbopack)로 만드는 한국어 반응형 웹 교육 퍼즐 게임입니다.
- Google 로그인 후 플레이하며, Firebase에 상세 게임·퀴즈 이력을 저장하고 오늘/역대 공개 리더보드를 제공합니다.
- MVP는 1개 레벨만 포함합니다. 4단계 난이도, HDD 화면, 멀티플레이·사운드는 후속 범위입니다.

## 게임 및 화면 구성

- 흐름: 로그인/시작 → 짧은 조작 안내 → 게임 → 결과·RAM 전원 끄기 시연 → 3문항 퀴즈 → 리더보드·내 기록.
- 한 판은 90초 동안 8비트 ASCII 목표 패킷 3개를 처리하는 방식입니다. 각 목표는 `01000001 → 65 → A`처럼 이진수·십진수·문자를 함께 보여 줍니다.
- RAM 보드는 목표 워드 8칸과 오염 데이터 버퍼 16칸으로 구성합니다. 다음 정답 비트를 강조된 목표 주소에 드래그하면 `+10점`, 8비트 완성 시 자동 처리 및 `+100점`을 얻습니다. 남은 시간은 초당 `+2점` 보너스입니다.
- 틀린 비트·미처리 비트는 버퍼에 오염 데이터로 남습니다. 전체 24칸이 차거나 제한 시간 내 목표 3개를 못 끝내면 실패합니다.
- `Flush`는 모든 RAM과 진행 중인 워드를 비우고 현재 패킷을 다시 시작하며 `-50점`입니다.
- 압축·클리어·지연 블록은 20초·45초·65초에 각각 한 번씩 떨어지고 전용 포트에 드롭해 사용합니다.
  - 압축: 오염 데이터 점유 칸을 절반 수준으로 줄입니다.
  - 클리어: 오염 데이터 버퍼만 비웁니다.
  - 지연: 6초간 낙하 속도를 절반으로 낮춥니다.
- 결과 화면은 점수, 처리 패킷/비트, 오염 데이터, 특수 블록 사용량을 보여 주고, “전원 끄기” 상호작용으로 남은 RAM 데이터가 사라지는 모습을 시연합니다. 이어지는 퀴즈의 문항별 답·정답 여부를 기록합니다.
- 네온 메모리 칩 스타일(어두운 배경, 청록·녹색 신호, 위험도 녹→황→적)을 CSS/SVG로 구현합니다. 드래그가 기본 조작이며, 터치와 클릭 대체 조작도 제공합니다.

## 구현 및 온라인 데이터

- 순수 게임 엔진을 UI와 분리하고 `RoundConfig`, `RoundEvent`, `GameState`, `VerifiedRound`, `QuizAttempt`, `LeaderboardEntry` 타입을 둡니다. 같은 시드와 이벤트 기록은 항상 동일한 결과를 재생해야 합니다.
- Firebase Authentication의 Google 로그인은 필수입니다. Google 표시 이름과 프로필 이미지를 사용하며, 표시 이름은 공개 리더보드에 그대로 노출됩니다.
- 로그인한 사용자의 브라우저가 직접 시드를 만들어 라운드를 시작하고, Realtime Database에 `issued` 상태로 기록합니다. 게임이 끝나면 같은 브라우저가 이벤트 기록을 엔진으로 재생해 점수·특수 블록 사용·시간을 계산하고 라운드를 `submitted` 상태로 기록합니다. 서버가 없으므로 이 재생은 클라이언트 자체 계산이며, 별도의 신뢰 가능한 검증 주체는 없습니다.
- Firebase Realtime Database에는 사용자 프로필, 게임별 결과, 문항별 퀴즈 이력, 오늘 및 역대 최고 점수 리더보드를 저장합니다. 오늘의 기준은 한국 표준시입니다.
- 리더보드는 성공한 판만 반영하며, 개인 이력은 모든 완료·실패 판을 보관합니다. 공개 리더보드는 누구나 읽을 수 있고, 쓰기는 `database.rules.json` 보안 규칙으로 제한합니다. 규칙은 본인 소유 데이터만, 라운드는 `issued → submitted` 순서로만, 리더보드 점수는 본인의 기존 최고 점수보다 높을 때만 쓰도록 허용합니다. 다만 규칙은 데이터 형태와 소유권만 확인할 뿐 점수가 실제 플레이로 얻어졌는지는 재현할 수 없으므로, 이전의 서버 재생 검증보다 부정행위 방지력이 약하다는 점은 감수한 선택입니다.
- “내 기록 전체 삭제”는 게임·퀴즈 이력과 개인 리더보드 항목을 삭제합니다. Google 로그인 계정 자체는 삭제하지 않으며, 다시 로그인하면 새 기록을 시작합니다.
- Firebase 설정값은 커밋하지 않습니다. 환경변수 예시와 Firebase 설정 안내를 제공하고, 설정값이 없을 때는 앱이 오류 없이 설정 안내 화면을 표시하게 합니다.
- 배포는 Firebase Hosting을 사용합니다. `next build`로 만든 정적 파일(`out/`)을 `firebase deploy --only hosting`으로 올리는 순수 정적 사이트이며, 서버 API가 없으므로 무료 Spark 요금제로 충분하고 Blaze(종량제) 요금제는 필요 없습니다.

## 검증 계획

- 게임 엔진 단위 테스트: 정답/오답 배치, 패킷 완료, RAM 포화, Flush, 압축·클리어·지연, 시간 종료, 점수 계산, 시드 재생.
- 서버 검증 테스트: 위조된 이벤트·중복 제출·불가능한 시간/점수 거부, 성공 판의 일간·역대 순위 갱신.
- Firebase Emulator 통합 테스트: Google 사용자 식별 모킹, 개인 이력 권한, 공개 리더보드 읽기, 삭제 처리와 보안 규칙.
- 브라우저 E2E 테스트: 로그인 → 드래그/터치 플레이 → 결과·휘발성 시연 → 퀴즈 저장 → 오늘/역대 리더보드 → 전체 기록 삭제.
- 반응형 및 접근성 점검: 모바일·데스크톱 레이아웃, 키보드/클릭 대체 조작, 타이머·오염 상태의 텍스트 안내.

## 가정 및 기본값

- Firebase 프로젝트는 아직 연결하지 않고, 구현 시 환경변수 템플릿과 설정 절차만 포함합니다.
- 외부 이미지·효과음은 사용하지 않고 코드 기반 시각 효과로 완성합니다.
- 점수·리더보드는 서버 재생 검증 없이 클라이언트가 계산해 직접 기록합니다. Firebase Blaze(종량제) 요금제 비용을 피하려는 선택이며, 그 대가로 리더보드가 honor-system에 가까워진다는 점을 감수합니다. 배포 시에는 Firebase 웹 앱·Google 로그인·Realtime Database·Firebase Hosting(정적) 설정만 있으면 됩니다.
