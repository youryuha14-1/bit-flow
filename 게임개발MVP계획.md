# Bit Flow MVP 개발 계획

## 요약

- Next.js(App Router·TypeScript·Tailwind·Turbopack)로 만드는 한국어 반응형 웹 교육 퍼즐 게임입니다.
- 로그인 없이 게임을 플레이할 수 있으며, 게임 종료 점수는 렛츠코딩 라운지 SDK를 통해 라운지 랭킹에 제출합니다.
- MVP는 1개 레벨만 포함합니다. 4단계 난이도, HDD 화면, 멀티플레이·사운드는 후속 범위입니다.

## 게임 및 화면 구성

- 흐름: 시작 → 짧은 조작 안내 → 게임 → 결과·랭킹 제출 상태·RAM 전원 끄기 시연 → 3문항 퀴즈 → 내 기록.
- 한 판은 90초 동안 8비트 ASCII 목표 패킷 3개를 처리하는 방식입니다. 각 목표는 `01000001 → 65 → A`처럼 이진수·십진수·문자를 함께 보여 줍니다.
- RAM 보드는 목표 워드 8칸과 오염 데이터 버퍼 16칸으로 구성합니다. 다음 정답 비트를 강조된 목표 주소에 드래그하면 `+10점`, 8비트 완성 시 자동 처리 및 `+100점`을 얻습니다. 남은 시간은 초당 `+2점` 보너스입니다.
- 틀린 비트·미처리 비트는 버퍼에 오염 데이터로 남습니다. 전체 24칸이 차거나 제한 시간 내 목표 3개를 못 끝내면 실패합니다.
- `Flush`는 모든 RAM과 진행 중인 워드를 비우고 현재 패킷을 다시 시작하며 `-50점`입니다.
- 압축·클리어·지연 블록은 20초·45초·65초에 각각 한 번씩 떨어지고 전용 포트에 드롭해 사용합니다.
  - 압축: 오염 데이터 점유 칸을 절반 수준으로 줄입니다.
  - 클리어: 오염 데이터 버퍼만 비웁니다.
  - 지연: 6초간 낙하 속도를 절반으로 낮춥니다.
- 결과 화면은 점수, 처리 패킷/비트, 오염 데이터, 특수 블록 사용량을 보여 주고, “전원 끄기” 상호작용으로 남은 RAM 데이터가 사라지는 모습을 시연합니다. 이어지는 퀴즈의 문항별 답·정답 여부를 기록합니다.
- 네온 메모리 칩 스타일(어두운 배경, 청록·녹색 신호, 위험도 녹→황→적)을 CSS/SVG로 구현합니다. 드래그가 기본 조작이며, 터치와 클릭 대체 조작도 제공합니다.

## 구현 및 랭킹 연동

- 순수 게임 엔진을 UI와 분리하고 `RoundConfig`, `RoundEvent`, `GameState`, `VerifiedRound` 타입을 둡니다. 같은 시드와 이벤트 기록은 항상 동일한 결과를 재생해야 합니다.
- 게임 자체 로그인 기능은 두지 않습니다. 랭킹 등록에 필요한 사용자 인증은 렛츠코딩 라운지의 로그인 세션을 사용하며, 로그인하지 않은 경우 SDK가 반환한 오류를 결과 화면에 그대로 표시합니다.
- 게임이 승리·실패·시간 초과로 완전히 종료되면 최종 `GameState.score`를 0 이상의 정수로 보정해 렛츠코딩 라운지 SDK의 `submitScore`에 한 번 제출합니다. 스테이지 진행, 일시정지처럼 이어서 플레이할 수 있는 상태에서는 제출하지 않습니다.
- SDK가 로드되지 않아도 게임은 계속 동작하고 결과 화면에 원인을 표시합니다. 네트워크 오류 등으로 제출이 실패하면 중복 방지 플래그를 되돌려 같은 판의 점수를 다시 시도할 수 있게 합니다.
- 사용자 프로필, 게임·퀴즈 이력, 랭킹 데이터를 앱의 별도 데이터베이스에 저장하지 않습니다. 화면의 최근 기록은 현재 브라우저 세션의 로컬 상태로만 관리하고, 실제 공개 랭킹은 렛츠코딩 라운지에서 관리합니다.
- Firebase 설정이나 환경변수는 사용하지 않습니다. 랭킹 SDK는 루트 레이아웃에서 외부 스크립트로 불러옵니다.
- 배포는 Vercel의 Next.js 네이티브 빌드를 사용합니다. 저장소를 가져올 때 프로젝트 Root Directory를 `bit-flow`로 지정하며, 별도 데이터베이스나 서버 환경변수는 필요하지 않습니다.

## 검증 계획

- 게임 엔진 단위 테스트: 정답/오답 배치, 패킷 완료, RAM 포화, Flush, 압축·클리어·지연, 시간 종료, 점수 계산, 시드 재생.
- 랭킹 연동 테스트: 최종 점수의 정수·최솟값 보정, 판당 중복 제출 방지, SDK 미로드 안내, 제출 실패 후 플래그 복구.
- 브라우저 통합 테스트: 렛츠코딩 SDK를 대체해 종료 조건별 제출 호출과 성공·401·네트워크 오류 상태 문구를 확인합니다.
- 브라우저 E2E 테스트: 시작 → 드래그/터치 플레이 → 결과·랭킹 제출 상태·휘발성 시연 → 퀴즈 → 내 기록.
- 반응형 및 접근성 점검: 모바일·데스크톱 레이아웃, 키보드/클릭 대체 조작, 타이머·오염 상태의 텍스트 안내.

## 가정 및 기본값

- 렛츠코딩 라운지 SDK는 배포 환경에서 제공되며, 로컬 개발에서는 SDK가 없더라도 게임과 결과 화면이 깨지지 않아야 합니다.
- 외부 이미지·효과음은 사용하지 않고 코드 기반 시각 효과로 완성합니다.
- 점수는 기존 게임 엔진이 클라이언트에서 계산하고, 완전히 종료된 판의 최종 값만 렛츠코딩 라운지에 제출합니다. 앱에는 별도 로그인·데이터베이스를 두지 않으며 Vercel의 Next.js 프로젝트로 배포합니다.
